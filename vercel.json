{
    "version": 2,
    "name": "express-test",
    "builds": [
      { "src": "server.js", "use": "@vercel/node" }
    ],
    "routes": [
      { "src": "/api/data", "dest": "/server.js" }, 
      { "src": "/api/:nom", "dest": "/server.js" }
    ]
}